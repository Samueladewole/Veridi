import { PageStub } from "@/components/dashboard/page-stub";

export default function ApiKeysPage() {
  return (
    <PageStub
      title="API Keys"
      icon="key"
      description="Manage production and sandbox API keys. Create, rotate, and revoke keys with granular endpoint permissions and IP whitelisting."
    />
  );
}
