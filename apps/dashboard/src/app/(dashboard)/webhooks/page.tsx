import { PageStub } from "@/components/dashboard/page-stub";

export default function WebhooksPage() {
  return (
    <PageStub
      title="Webhooks"
      icon="bell"
      description="Configure webhook endpoints, view delivery logs, and manage retry policies. Test webhooks with sandbox events before going live."
    />
  );
}
