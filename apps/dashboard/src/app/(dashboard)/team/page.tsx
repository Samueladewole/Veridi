import { PageStub } from "@/components/dashboard/page-stub";

export default function TeamPage() {
  return (
    <PageStub
      title="Team"
      icon="users"
      description="Invite team members, assign roles, and manage permissions. Control who can access production keys, view billing, and manage verifications."
    />
  );
}
