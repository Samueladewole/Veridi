import { PageStub } from "@/components/dashboard/page-stub";

export default function AuditLogPage() {
  return (
    <PageStub
      title="Audit Log"
      icon="scroll"
      description="Complete audit trail of all API calls, key rotations, team changes, and configuration updates. Filter by date, user, and event type."
    />
  );
}
