import { PageStub } from "@/components/dashboard/page-stub";

export default function AddressVerificationPage() {
  return (
    <PageStub
      title="Address Verification"
      icon="map-pin"
      description="Physical address verification with agent dispatch and utility bill cross-referencing. Verify Nigerian addresses with geo-tagged evidence."
    />
  );
}
