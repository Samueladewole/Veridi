import { PageStub } from "@/components/dashboard/page-stub";

export default function VeridiIdPage() {
  return (
    <PageStub
      title="Veridi ID"
      icon="id-card"
      description="NIN and BVN identity verification. Submit single or batch verification requests and view detailed match results."
    />
  );
}
