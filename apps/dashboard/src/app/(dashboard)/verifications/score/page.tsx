import { PageStub } from "@/components/dashboard/page-stub";

export default function VeridiScorePage() {
  return (
    <PageStub
      title="Veridi Score"
      icon="star"
      description="Composite identity trust score combining NIN, BVN, facial, and background verification signals into a single 0-100 confidence score."
    />
  );
}
