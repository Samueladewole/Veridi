import { PageStub } from "@/components/dashboard/page-stub";

export default function LiveCallsPage() {
  return (
    <PageStub
      title="Live Calls"
      icon="zap"
      description="Real-time streaming feed of verification calls as they happen. Monitor live latency, success rates, and catch issues instantly."
    />
  );
}
