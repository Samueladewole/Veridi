import { PageStub } from "@/components/page-stub";

export default function WebhookMonitorPage() {
  return (
    <PageStub
      title="Webhook Monitor"
      description="Track outbound webhook delivery to client endpoints. Monitor success rates, retry queues, and failed deliveries with detailed request/response logs."
      icon="\uD83D\uDD14"
    />
  );
}
