import { PageStub } from "@/components/page-stub";

export default function LiveFeedPage() {
  return (
    <PageStub
      title="Live Feed"
      description="Real-time API call monitoring across all clients. View verification requests, response times, and error rates as they happen."
      icon="\u26A1"
    />
  );
}
