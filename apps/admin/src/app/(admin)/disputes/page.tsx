import { PageStub } from "@/components/page-stub";

export default function DisputesPage() {
  return (
    <PageStub
      title="Disputes"
      description="Manage client disputes on verification results. Track escalations, review evidence, and record resolutions for compliance."
      icon="\u26A0"
    />
  );
}
