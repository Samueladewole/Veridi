import { PageStub } from "@/components/page-stub";

export default function ConfigurationPage() {
  return (
    <PageStub
      title="Configuration"
      description="Platform-wide settings including rate limits, provider configurations, notification preferences, feature flags, and admin user management."
      icon="\u2699"
    />
  );
}
