import { PageStub } from "@/components/page-stub";

export default function AuditLogPage() {
  return (
    <PageStub
      title="Audit Log"
      description="Immutable record of all administrative actions. Every approval, rejection, configuration change, and system event is permanently logged for compliance."
      icon="\uD83D\uDCDC"
    />
  );
}
