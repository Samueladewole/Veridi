import { PageStub } from "@/components/page-stub";

export default function VerificationQueuePage() {
  return (
    <PageStub
      title="Verification Queue"
      description="Review pending verifications that require manual approval. NIN mismatches, flagged background checks, and edge cases await your decision."
      icon="\uD83D\uDDC2"
    />
  );
}
